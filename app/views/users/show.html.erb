<div class="container">
  <div class="row justify-content-center">
    <div class="card col-12 col-md-4">
      <div class="card-body">
        <h1><%= @user.name %></h1>
        <%= image_tag @user.avatar, class: "user-avatar img-thumbnail" %>
        <h6 class="card-subtitle mb-2 text-muted"><%= @user.email %></h6>
        <% if @user.gender == "male" %>
          <h6>男</h6>
        <% elsif @user.gender == "female" %>
          <h6>女</h6>
        <% end %>
        <h6 class="card-text">自我介紹:</h6>
        <p><%= @user.introduction %></p>
        <% if @user.email == current_user.email %>
          <%= link_to '更新個人資料', edit_user_path(@user), class: "btn btn-success" %>
          <%= link_to '修改密碼', edit_user_registration_path(@user), class: "btn btn-warning" %>
        <% end %>
      </div>
    </div>
    <div class="col-12 col-md-6">
      <ul class="list-group">
        <li class="list-group-item list-group-item-light"></li>
        <li class="list-group-item list-group-item-warning text-center">貢獻了<%= @contributed_events.count %>個行程！</li>
        <li class="list-group-item list-group-item-light"></li>
        <li class="list-group-item list-group-item-info text-center">恭喜！被複製了<%= @cloned_events.count %>個行程！</li>
        <li class="list-group-item list-group-item-light"></li>
        <li class="list-group-item list-group-item-success text-center">收藏了<%= @favorited_events.count %>個行程！</li>
        <li class="list-group-item list-group-item-light"></li>
      </ul>
      <div class="card border-info mb-3">
        <div class="card-header text-center">目前積分</div>
          <div class="card-body text-info">
          <h2 class="card-title"><%= @point %>分！</h2>
          <p class="card-text">1個行程被複製2分，貢獻1個行程1分</p>
        </div>
      </div>
    </div>
  </div>
</div>