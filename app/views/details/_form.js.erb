 function reload(scheduleId){
      $.ajax({
        url: "/schedules/" + scheduleId,
        method: "GET",
        dataType: "HTML",

        error: function (xhr) {
          alert("error");
        },
        success: function (data) {
          $("#schedule-review").html(data);
          initialize(<%= raw @schedules.spots.to_json %>)
          alert("<%=@msgResult%>");


        }
      });
    }
    reload(<%=@detail.schedule.id%>);
