<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="navbar-brand">
    <%= link_to "Travel World", root_path, id: "logo" %>
  </div>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav mr-auto">
      <% if current_user %>
        <% if current_user&.admin? %>
          <li class="nav-item"><%= link_to 'Admin Panel', admin_tweets_path %></li>
        <% end %>
        <li class="nav-item"><%= link_to('登出', destroy_user_session_path, method: :delete) %></li>
      <% else %>
        <li class="nav-item"><%= link_to('註冊', new_user_registration_path) %></li>
        <li class="nav-item"><%= link_to('登入', new_user_session_path) %></li>
      <% end %>
    </ul>
  </div>
  <form class="form-inline">
    <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
    <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
  </form>
</nav>
